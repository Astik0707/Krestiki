# 🚀 Инструкция по настройке Telegram на Netlify

## ❌ Проблема: Telegram уведомления не работают

Если уведомления не отправляются, проверьте следующие пункты:

---

## ✅ Шаг 1: Настройка переменных окружения на Netlify

1. **Войдите в панель Netlify:**
   - Перейдите на https://app.netlify.com
   - Выберите ваш сайт `krestikinolik`

2. **Откройте настройки переменных окружения:**
   - В меню сайта: **Site settings** → **Environment variables**
   - Или: **Site configuration** → **Environment variables**

3. **Добавьте переменную:**
   - Нажмите **"Add a variable"**
   - **Key (ключ):** `TELEGRAM_BOT_TOKEN`
   - **Value (значение):** `ваш_токен_бота` (например: `YOUR_BOT_TOKEN_HERE`)
   - Нажмите **"Save"**

4. **Пересоберите сайт:**
   - Перейдите в **Deploys**
   - Нажмите **"Trigger deploy"** → **"Clear cache and deploy site"**
   - Дождитесь завершения сборки

---

## ✅ Шаг 2: Проверка работы API

1. **Откройте консоль браузера:**
   - На сайте нажмите `F12`
   - Перейдите на вкладку **Console**

2. **Сыграйте игру и проверьте логи:**
   - При победе/проигрыше в консоли должны появиться логи
   - Ищите сообщения типа: `"Отправка сообщения в Telegram"` или `"Ошибка отправки в Telegram"`

3. **Проверьте Network (Сеть):**
   - Вкладка **Network** → найдите запрос к `/api/telegram`
   - Посмотрите ответ сервера (Response)

---

## ✅ Шаг 3: Проверка активации бота

**Важно:** Пользователь должен сначала написать боту!

1. **Откройте Telegram**
2. **Найдите бота:** `@krestiki_i_noliki01_bot`
3. **Напишите боту любое сообщение** (например: "Привет" или "/start")
4. **Попробуйте снова сыграть в игру**

---

## ✅ Шаг 4: Использование chat_id вместо username

Username может не работать, если пользователь давно не писал боту. Используйте chat_id:

1. **Узнайте ваш chat_id:**
   - Напишите боту `@userinfobot` в Telegram
   - Он пришлет ваш `Id` (это и есть chat_id)

2. **Введите chat_id в игре:**
   - В модальном окне введите только число (без @)
   - Например: `565133375`

---

## 🔍 Диагностика проблем

### Проблема: "Telegram bot token not configured"

**Решение:**
- Переменная `TELEGRAM_BOT_TOKEN` не настроена на Netlify
- См. Шаг 1 выше

### Проблема: "Chat not found" или "chat not found"

**Решение:**
- Пользователь не писал боту
- Напишите боту `@krestiki_i_noliki01_bot` любое сообщение
- Используйте chat_id вместо username

### Проблема: "bot was blocked"

**Решение:**
- Разблокируйте бота в Telegram
- Настройки → Приватность → Заблокированные пользователи

### Проблема: API возвращает 404

**Решение:**
- Убедитесь, что файл `netlify.toml` есть в репозитории
- Пересоберите сайт на Netlify

---

## 📝 Быстрая проверка

1. ✅ Переменная `TELEGRAM_BOT_TOKEN` настроена на Netlify
2. ✅ Сайт пересобран после добавления переменной
3. ✅ Пользователь написал боту `@krestiki_i_noliki01_bot`
4. ✅ В игре указан правильный username или chat_id
5. ✅ Консоль браузера не показывает ошибок

---

## 🔧 Дополнительно: Проверка через API напрямую

Можно проверить работу API напрямую:

```bash
curl -X POST https://krestikinolik.netlify.app/api/telegram \
  -H "Content-Type: application/json" \
  -d '{
    "message": "Тест",
    "chatId": "ваш_chat_id"
  }'
```

Или через браузер (откройте консоль и выполните):

```javascript
fetch('/api/telegram', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    message: 'Тест',
    chatId: 'ваш_chat_id'
  })
}).then(r => r.json()).then(console.log)
```

---

## 📞 Если ничего не помогает

1. Проверьте логи Netlify:
   - **Site settings** → **Build & deploy** → **Deploy log**
   - Ищите ошибки при сборке

2. Проверьте функции Netlify:
   - **Functions** → должны быть функции из `/api/telegram`

3. Убедитесь, что токен бота правильный:
   - Проверьте в @BotFather

